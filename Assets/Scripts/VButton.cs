using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VButton : MonoBehaviour
{

    // Text to change

    // VText Allows the text to animate
      // Transition pages
      // Change size
      // Change color
      // etc

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
